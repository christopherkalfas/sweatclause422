  
<p id="notice"><%= notice %></p>

<h1>Users</h1>
  <ul class="list-group list-group-flush">
    <% @users.each do |user| %>      
        <li class="list-group-item"><%= link_to(user.username, user_path(user)) %></li>
    <% end %>
  </ul>


<br>

<h2><%= link_to('New User', new_user_path) %></h2>